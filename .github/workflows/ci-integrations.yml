name: Smoke Tests

on:
  push:
    branches: ["main", "release/*", "develop"]
  pull_request:
    branches: ["main", "release/*", "develop"]

permissions:
  contents: read

env:
  # UV_TORCH_BACKEND only works with 'uv pip' commands, not 'uv sync'.
  # Used by 'uv pip install torch torchvision' step to install CPU-only PyTorch.
  UV_TORCH_BACKEND: "cpu"

jobs:
  try-all-models:
    runs-on: ${{ matrix.os }}
    concurrency:
      group: model-smoke-${{ matrix.os }}-${{ matrix.python-version }}-${{ github.ref }}
      cancel-in-progress: ${{ github.event_name == 'pull_request' }}
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu-latest", "macos-latest", "windows-latest"]
        python-version: ["3.10", "3.13"]
    timeout-minutes: 15
    steps:
      - name: 📥 Checkout the repository
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - name: 🐍 Install uv and set Python version ${{ matrix.python-version }}
        uses: astral-sh/setup-uv@61cb8a9741eeb8a550a1b8544337180c0fc8476b # v7.2.0
        with:
          version: "0.9.26"
          python-version: ${{ matrix.python-version }}
          activate-environment: true

      - name: 🚀 Install Packages (plus extras)
        timeout-minutes: 5
        # Install PyTorch CPU-only first (UV_TORCH_BACKEND=cpu works with 'uv pip')
        run: uv pip install -e .[plus]

      - name: 🔎 Smoke-test model instantiation, downloads, and inference
        run: python tests/run_smoke_all_models.py

      - name: Minimize uv cache
        run: uv cache prune --ci

  executorch-parity:
    name: ExecuTorch parity
    # The real numerical-parity test exports an RFDETRNano to .pte and asserts the
    # XNNPACK-delegated runtime matches eager PyTorch. XNNPACK is a plain-CPU backend,
    # so no special hardware/SDK is required — ubuntu-latest is sufficient.
    runs-on: ${{ matrix.os }}
    concurrency:
      group: pytest-executorch-${{ matrix.python-version }}-${{ github.ref }}
      cancel-in-progress: ${{ github.event_name == 'pull_request' }}
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu-latest"]
        # executorch 1.3.1 ships manylinux_2_28 x86_64 wheels for cp310–cp313 and
        # requires_python is >=3.10,<3.14. 3.10 (floor) and 3.13 (ceiling) bracket
        # the supported range; widen the matrix if the [executorch] pin changes.
        python-version: ["3.10", "3.13"]
    steps:
      - name: 📥 Checkout the repository
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - name: 🐍 Install uv and set Python version ${{ matrix.python-version }}
        uses: astral-sh/setup-uv@61cb8a9741eeb8a550a1b8544337180c0fc8476b # v7.2.0
        with:
          version: "0.9.26"
          python-version: ${{ matrix.python-version }}
          activate-environment: true

      - name: 🚀 Install Packages (executorch extra)
        timeout-minutes: 8
        # The end-to-end parity test builds and exports the full RFDETRNano stack, so
        # pull the same extras as the CPU test job plus the [executorch] backend.
        # --group ci-executorch-pin locks torch and executorch to a pair whose C10 ABI
        # matches (torch 2.12.x + executorch 1.3.1; see pyproject.toml for the empirically
        # validated range and removal condition).
        run: uv pip install -e ".[executorch,train,augment,cli,visual]" --group tests --group ci-executorch-pin

      - name: 🔎 Verify executorch runtime imports
        # Guard against a silently-skipped parity test: the test class is gated by a
        # skipif on executorch availability, so a broken/missing wheel would skip it
        # (false green). Fail here (red) instead if the runtime cannot be imported.
        # require_runtime=True also surfaces an actionable ABI-compatibility message
        # (rather than a bare "undefined symbol") if the pin above ever drifts stale.
        # shell: python — activate-environment: true (setup-uv, above) puts the venv's
        # python on PATH, so this runs the script through it directly (no uv run needed).
        run: |
          from rfdetr.export._executorch import _IS_EXECUTORCH_AVAILABLE
          from rfdetr.export._executorch.exporter import _check_executorch_available

          _check_executorch_available(require_runtime=True)
          assert _IS_EXECUTORCH_AVAILABLE, "executorch installed but not detected as available"
          print("executorch runtime import OK")
        shell: python

      - name: 🧪 Run ExecuTorch parity tests
        # executorch export is heavy; run single-worker as advised for this suite.
        run: |
          uv run --no-sync pytest tests/export/test_executorch_export.py \
            -m e2e_executorch -n 1 \
            --timeout=600 \
            --durations=20

      - name: Minimize uv cache
        continue-on-error: true
        run: uv cache prune --ci

  coreml-parity:
    name: CoreML parity
    # coremltools + the CoreML runtime are macOS-only, so unlike executorch-parity (portable
    # XNNPACK, runs on ubuntu-latest) this job requires macos-latest.
    runs-on: macos-latest
    concurrency:
      group: pytest-coreml-${{ matrix.python-version }}-${{ github.ref }}
      cancel-in-progress: ${{ github.event_name == 'pull_request' }}
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        # 3.11 not 3.10 for this job specifically (bumped alongside the coreml extra's torch<2.12
        # pin, see pyproject.toml) — other CI jobs keep testing 3.10 as the project floor.
        python-version: ["3.11", "3.13"]
    steps:
      - name: 📥 Checkout the repository
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - name: 🐍 Install uv and set Python version ${{ matrix.python-version }}
        uses: astral-sh/setup-uv@61cb8a9741eeb8a550a1b8544337180c0fc8476b # v7.2.0
        with:
          version: "0.9.26"
          python-version: ${{ matrix.python-version }}
          activate-environment: true

      - name: 🚀 Install Packages (coreml extra)
        timeout-minutes: 8
        run: uv pip install -e ".[coreml,train,augment,cli,visual]" --group tests

      - name: 🔎 Verify coremltools import
        # Guard against a silently-skipped parity test: TestCoreMLEndToEnd is gated by a
        # skipif on coremltools availability, so a broken/missing wheel would skip it (false
        # green) instead of failing red.
        run: |
          from rfdetr.export._coreml import _IS_COREMLTOOLS_AVAILABLE

          assert _IS_COREMLTOOLS_AVAILABLE, "coremltools installed but not detected as available"
          print("coremltools import OK")
        shell: python

      - name: 🧪 Run CoreML end-to-end parity tests
        # Real ct.convert + mlmodel.predict + download_assets is heavy/network; single-worker,
        # opt-in marker (see pyproject.toml `e2e_coreml`) — the class also carries the shared
        # `integration` marker, excluded by ci-tests-cpu.yml (`and not integration`).
        run: |
          uv run --no-sync pytest tests/export/test_coreml_export.py \
            -m e2e_coreml -n 1 \
            --timeout=600 \
            --durations=20

      - name: Minimize uv cache
        continue-on-error: true
        run: uv cache prune --ci

  tensorrt-parity:
    name: TensorRT parity
    # TensorRT engine build + runtime inference need a real GPU + CUDA driver, so unlike the CPU
    # integration jobs this runs on the self-hosted GPU runner (same as ci-tests-gpu.yml).
    runs-on: Roboflow-GPU-VM-Runner
    timeout-minutes: 25
    concurrency:
      group: pytest-tensorrt-${{ github.ref }}
      cancel-in-progress: ${{ github.event_name == 'pull_request' }}
    env:
      # UV_TORCH_BACKEND=auto (GPU) works only with uv pip, not uv sync; overrides the workflow-level
      # "cpu" default which is wrong for this GPU job.
      UV_TORCH_BACKEND: "auto"
    steps:
      - name: 🖥️ Print GPU information
        run: nvidia-smi
      - name: 📥 Checkout the repository
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - name: 🐍 Install uv and set Python version
        uses: astral-sh/setup-uv@61cb8a9741eeb8a550a1b8544337180c0fc8476b # v7.2.0
        with:
          version: "0.9.26"
          python-version: "3.12"
          activate-environment: true

      - name: 🚀 Install Packages (tensorrt + onnx extras)
        timeout-minutes: 8
        # [tensorrt] provides tensorrt/polygraphy for the engine build; [onnx] provides the ONNX
        # export toolchain (the e2e exports RFDETRNano to ONNX first).
        run: uv pip install -e ".[tensorrt,onnx,train,augment,cli,visual]" --group tests

      - name: 🔎 Verify tensorrt import
        # Guard against a silently-skipped parity test: TestTensorRTEndToEnd is gated by a skipif on
        # tensorrt availability, so a broken/missing wheel would skip it (false green) instead of failing red.
        run: |
          from rfdetr.export._tensorrt.exporter import _IS_TENSORRT_AVAILABLE

          assert _IS_TENSORRT_AVAILABLE, "tensorrt/polygraphy installed but not detected as available"
          print("tensorrt import OK")
        shell: python

      - name: 🔗 Expose CUDA runtime (libcudart.so) to the loader
        # The engine build succeeds, but polygraphy's TrtRunner separately dlopen's the *unversioned*
        # `libcudart.so` for CUDA stream management. The GPU wheels ship it only as `libcudart.so.12`
        # (e.g. under torch/lib), which the dynamic loader does not resolve by bare name — hence
        # `OSError: libcudart.so: cannot open shared object file` at runtime. Symlink the versioned
        # library to `libcudart.so` in a job-local dir and prepend it to LD_LIBRARY_PATH.
        run: |
          import glob
          import os
          import pathlib
          import site

          roots = list(site.getsitepackages())
          venv = os.environ.get("VIRTUAL_ENV")
          if venv:
              roots.append(os.path.join(venv, "lib"))
          candidates = []
          for root in roots:
              candidates += glob.glob(os.path.join(root, "**", "libcudart.so*"), recursive=True)
          candidates += glob.glob("/usr/local/cuda*/lib64/libcudart.so*")
          candidates = sorted({c for c in candidates if os.path.exists(c)}, key=len)
          assert candidates, "libcudart.so* not found in installed wheels or system CUDA"
          source = candidates[0]

          lib_dir = pathlib.Path(os.environ["RUNNER_TEMP"]) / "cudalibs"
          lib_dir.mkdir(parents=True, exist_ok=True)
          link = lib_dir / "libcudart.so"
          if link.is_symlink() or link.exists():
              link.unlink()
          link.symlink_to(source)
          print(f"linked {link} -> {source}")

          with open(os.environ["GITHUB_ENV"], "a", encoding="utf-8") as env_file:
              env_file.write(f"LD_LIBRARY_PATH={lib_dir}{os.pathsep}{os.environ.get('LD_LIBRARY_PATH', '')}\n")
        shell: python

      - name: 🧪 Run TensorRT end-to-end parity tests
        # Real ONNX->engine build + GPU inference is heavy; single-worker, opt-in marker (see pyproject.toml
        # `e2e_tensorrt`) — excluded from the ci-tests-gpu.yml default filter (`and not e2e_tensorrt`) so it
        # only runs here.
        run: |
          uv run --no-sync pytest tests/export/test_tensorrt_export.py \
            -m e2e_tensorrt -n 1 \
            --timeout=600 \
            --durations=20

      - name: Minimize uv cache
        continue-on-error: true
        run: uv cache prune --ci

  roboflow-deploy-e2e:
    name: Roboflow deploy e2e
    runs-on: ubuntu-latest
    # deploy_to_roboflow() delegates the actual upload to a swallow-and-print try/except deep in
    # the roboflow SDK (see tests/export/test_roboflow_deploy_e2e.py module docstring), so a failed
    # server-side upload is otherwise indistinguishable from success. This job generates a fresh
    # dataset version, deploys against a real Roboflow workspace/project (version auto-resolved to
    # that fresh latest), and independently polls that version's trained-model status.
    # Live external service + real account state: advisory only, not a required check (mirrors
    # ci-legacy-checkpoints.yml). Restricted to the upstream repo — forks never have the secret, so
    # running there would just burn CI minutes on a guaranteed skip.
    if: >-
      github.repository == 'roboflow/rf-detr' &&
      (github.event_name != 'pull_request' ||
       github.event.pull_request.head.repo.full_name == github.repository)
    concurrency:
      # Repo-wide group, not per-ref: two parallel runs against the same Roboflow project race —
      # each generates its own fresh version, so run A's auto-latest deploy can land on run B's
      # newer version while A polls its own, guaranteeing a 300s timeout false-negative. Queue
      # instead (and never cancel mid-upload, which would strand a half-deployed version).
      group: roboflow-deploy-e2e
      cancel-in-progress: false
    timeout-minutes: 20
    steps:
      - name: 📥 Checkout the repository
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - name: 🐍 Install uv and set Python version
        uses: astral-sh/setup-uv@61cb8a9741eeb8a550a1b8544337180c0fc8476b # v7.2.0
        with:
          version: "0.9.26"
          python-version: "3.13"
          activate-environment: true

      - name: 🚀 Install Packages (train extra, for the roboflow SDK)
        timeout-minutes: 5
        run: uv pip install -e ".[train]" --group tests

      - name: 🔑 Verify required secrets are present
        run: |
          missing=()
          [[ -z "${{ secrets.ROBOFLOW_API_KEY }}" ]]        && missing+=(ROBOFLOW_API_KEY)
          [[ -z "${{ secrets.ROBOFLOW_TEST_WORKSPACE }}" ]] && missing+=(ROBOFLOW_TEST_WORKSPACE)
          [[ -z "${{ secrets.ROBOFLOW_TEST_PROJECT }}" ]]   && missing+=(ROBOFLOW_TEST_PROJECT)
          if [[ ${#missing[@]} -gt 0 ]]; then
            echo "::error::Required secrets are not set: ${missing[*]}"
            exit 1
          fi

      - name: 🔎 Deploy a real model and poll for trained status
        env:
          ROBOFLOW_API_KEY: ${{ secrets.ROBOFLOW_API_KEY }}
          ROBOFLOW_TEST_WORKSPACE: ${{ secrets.ROBOFLOW_TEST_WORKSPACE }}
          ROBOFLOW_TEST_PROJECT: ${{ secrets.ROBOFLOW_TEST_PROJECT }}
        run: |
          uv run --no-sync pytest tests/ -m e2e_roboflow -n 1 \
            --timeout=600 \
            --durations=20

      - name: Minimize uv cache
        continue-on-error: true
        run: uv cache prune --ci

  openvino-parity:
    name: OpenVINO parity
    # openvino ships portable manylinux wheels (no macOS/GPU-only constraint like coreml/tensorrt),
    # so this can run on ubuntu-latest like executorch-parity.
    runs-on: ubuntu-latest
    concurrency:
      group: pytest-openvino-${{ matrix.python-version }}-${{ github.ref }}
      cancel-in-progress: ${{ github.event_name == 'pull_request' }}
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.13"]
    steps:
      - name: 📥 Checkout the repository
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - name: 🐍 Install uv and set Python version ${{ matrix.python-version }}
        uses: astral-sh/setup-uv@61cb8a9741eeb8a550a1b8544337180c0fc8476b # v7.2.0
        with:
          version: "0.9.26"
          python-version: ${{ matrix.python-version }}
          activate-environment: true

      - name: 🚀 Install Packages (openvino extra)
        timeout-minutes: 8
        run: uv pip install -e ".[openvino,train,augment,cli,visual]" --group tests

      - name: 🔎 Verify openvino import
        # Guard against a silently-skipped parity test: TestOpenVINOEndToEnd is gated by
        # pytest.importorskip("openvino"), so a broken/missing wheel would skip it (false
        # green) instead of failing red.
        run: |
          from rfdetr.export._openvino import _IS_OPENVINO_AVAILABLE

          assert _IS_OPENVINO_AVAILABLE, "openvino installed but not detected as available"
          print("openvino import OK")
        shell: python

      - name: 🧪 Run OpenVINO end-to-end parity tests
        # Real convert_model + save_model + Core().compile_model is heavy; single-worker,
        # opt-in marker (see pyproject.toml `e2e_openvino`) — the class also carries the shared
        # `integration` marker, excluded by ci-tests-cpu.yml (`and not integration`).
        run: |
          uv run --no-sync pytest tests/ -m e2e_openvino -n 1 \
            --timeout=600 --durations=20

      - name: Minimize uv cache
        continue-on-error: true
        run: uv cache prune --ci

  litert-parity:
    name: LiteRT parity
    # Real litert-torch convert + ai_edge_litert interpreter run on CPU (XNNPACK): no special hardware,
    # ubuntu-latest is sufficient. litert-torch is a pure-Python wheel; its ai-edge-litert / litert-converter
    # companions ship manylinux x86_64 wheels for cp310-cp314, and its own metadata pins torch<2.14.
    runs-on: ubuntu-latest
    concurrency:
      group: pytest-litert-${{ matrix.python-version }}-${{ github.ref }}
      cancel-in-progress: ${{ github.event_name == 'pull_request' }}
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.13"]
    steps:
      - name: 📥 Checkout the repository
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - name: 🐍 Install uv and set Python version ${{ matrix.python-version }}
        uses: astral-sh/setup-uv@61cb8a9741eeb8a550a1b8544337180c0fc8476b # v7.2.0
        with:
          version: "0.9.26"
          python-version: ${{ matrix.python-version }}
          activate-environment: true

      - name: 🚀 Install Packages (litert extra)
        timeout-minutes: 8
        run: uv pip install -e ".[litert,train,augment,cli,visual]" --group tests

      - name: 🔎 Verify litert-torch import
        # Guard against a silently-skipped parity test: TestLiteRTEndToEnd is gated by
        # pytest.importorskip("litert_torch"), so a broken/missing wheel would skip it (false
        # green) instead of failing red. Calls the checker itself rather than asserting the
        # package-level flag: the flag folds any ImportError from litert-torch's own import tree
        # into a bare False, while the checker re-raises it with the chained cause, so the log
        # shows which transitive package broke instead of only "not detected".
        run: |
          from rfdetr.export._litert.exporter import _check_litert_available

          _check_litert_available()
          print("litert-torch import OK")
        shell: python

      - name: 🧪 Run LiteRT end-to-end parity tests
        # Real litert_torch.convert + interpreter runs are heavy; single-worker, opt-in marker (see
        # pyproject.toml `e2e_litert`) — the class also carries the shared `integration` marker,
        # excluded by ci-tests-cpu.yml (`and not integration`).
        run: |
          uv run --no-sync pytest tests/ -m e2e_litert -n 1 \
            --timeout=600 --durations=20

      - name: Minimize uv cache
        continue-on-error: true
        run: uv cache prune --ci

  onnxruntime-session:
    name: ONNX Runtime session
    # tests/export/test_onnx_inference.py builds a real onnxruntime CPU session through _create_onnx_session.
    # Neither test workflow reaches those tests: ci-tests-cpu.yml does not install the [onnx] extra, so they
    # skip behind pytest.importorskip, and ci-tests-gpu.yml installs it but collects only `-m gpu`. This job
    # is the CPU path that turns a regression in the session options red. onnxruntime ships portable
    # manylinux wheels, so ubuntu-latest is sufficient.
    runs-on: ubuntu-latest
    concurrency:
      group: pytest-onnxruntime-${{ matrix.python-version }}-${{ github.ref }}
      cancel-in-progress: ${{ github.event_name == 'pull_request' }}
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        # The [onnx] extra declares two onnxruntime resolutions (see pyproject.toml): `<1.24` on 3.10, whose
        # last cp310 release 1.23.2 loads models up to IR 11, and unpinned on 3.11+. 3.10 and 3.13 bracket
        # both, so a model the tests build has to load under each.
        python-version: ["3.10", "3.13"]
    steps:
      - name: 📥 Checkout the repository
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - name: 🐍 Install uv and set Python version ${{ matrix.python-version }}
        uses: astral-sh/setup-uv@61cb8a9741eeb8a550a1b8544337180c0fc8476b # v7.2.0
        with:
          version: "0.9.26"
          python-version: ${{ matrix.python-version }}
          activate-environment: true

      - name: 🚀 Install Packages (onnx extra)
        timeout-minutes: 8
        run: uv pip install -e ".[onnx,train,augment,cli,visual]" --group tests

      - name: 🔎 Verify onnx and onnxruntime imports
        # Guard against a silently-skipped test: the real-session tests are gated by
        # pytest.importorskip("onnx") / pytest.importorskip("onnxruntime"), so a broken/missing wheel
        # would skip them (false green) instead of failing red.
        run: |
          import onnx
          import onnxruntime

          print(f"onnx {onnx.__version__} (IR {onnx.IR_VERSION}) / onnxruntime {onnxruntime.__version__} import OK")
        shell: python

      - name: 🧪 Run ONNX Runtime session tests
        # The dedicated marker keeps this integration-only coverage independent from file layout and
        # prevents unrelated ONNX inference unit tests from becoming an accidental CI requirement.
        run: |
          uv run --no-sync pytest tests/ -m e2e_onnxruntime -n 1 \
            --cov=rfdetr --cov-report=xml \
            --timeout=300 --durations=20

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@75cd11691c0faa626561e295848008c8a7dddffe # v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: "coverage.xml"
          flags: integration,onnxruntime,${{ runner.os }},py${{ matrix.python-version }}
          env_vars: OS,PYTHON
          name: codecov-umbrella
          fail_ci_if_error: false

      - name: Minimize uv cache
        continue-on-error: true
        run: uv cache prune --ci

  export-priority-guardian:
    runs-on: ubuntu-latest
    needs: [executorch-parity, coreml-parity, openvino-parity, tensorrt-parity, litert-parity, onnxruntime-session]
    if: always()
    steps:
      - name: 📋 Display export job results
        run: |
          echo "executorch-parity:   ${{ needs.executorch-parity.result }}"
          echo "coreml-parity:       ${{ needs.coreml-parity.result }}"
          echo "openvino-parity:     ${{ needs.openvino-parity.result }}"
          echo "tensorrt-parity:     ${{ needs.tensorrt-parity.result }}"
          echo "litert-parity:       ${{ needs.litert-parity.result }}"
          echo "onnxruntime-session: ${{ needs.onnxruntime-session.result }}"
      # Fail explicitly on any non-success result (failure, cancelled, or skipped) instead of
      # relying on timeout behavior, so a cancelled or skipped export job still blocks the gate.
      - name: ❌ Fail guardian unless every export job succeeded
        if: >-
          needs.executorch-parity.result != 'success' ||
          needs.coreml-parity.result != 'success' ||
          needs.openvino-parity.result != 'success' ||
          needs.tensorrt-parity.result != 'success' ||
          needs.litert-parity.result != 'success' ||
          needs.onnxruntime-session.result != 'success'
        run: |
          echo "One or more export jobs did not succeed; failing explicitly."
          exit 1
      - name: ✅ all export jobs succeeded
        run: echo "All export jobs (executorch/coreml/openvino/tensorrt/litert parity, onnxruntime session) completed successfully."
